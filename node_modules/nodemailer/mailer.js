const nodemailer = require('nodemailer');

// setup the transporter object
let transporter = nodemailer.createTransport({
  service: 'gmail',
  auth: {
    user: 'aktesting98@gmail.com',
    pass: 'testMailing.98'
  }
});

// setup the mailOptions
let mailOptions = {
  from: 'aktesting98@gmail.com',
  to: 'infernoxl98@gmail.com',
  subject: "Testing one last time",
  text: 'This is working',
  attachments: [
    {
      file: 'test.txt',
      path: __dirname+'/test.txt'
      //path: folderName + "/" + fileName
    }
  ]
};

// sending the email, with validation of success
transporter.sendMail(mailOptions, function(err, data) {
  if (err) {
    console.log('Error Occurs', err);
  } else {
    console.log('Email sent!!!');
  }
});


// working code (except for attachments)
/*
"use strict";
const nodemailer = require("nodemailer");

const testAccount = {
    user: "emilia.russel87@ethereal.email",
    pass: "QTGRa3y7yrHHxbMMKg"

}

// async..await is not allowed in global scope, must use a wrapper
async function main() {
  // Generate test SMTP service account from ethereal.email
  // Only needed if you don't have a real mail account for testing
  //let testAccount = await nodemailer.createTestAccount();

  // create reusable transporter object using the default SMTP transport
  let transporter = nodemailer.createTransport({
    host: "smtp.ethereal.email",
    port: 587,
    secure: false, // true for 465, false for other ports
    auth: {
      user: testAccount.user, // generated ethereal user
      pass: testAccount.pass, // generated ethereal password
    },
  });
  
  // send mail with defined transport object
  let info = await transporter.sendMail({
    from: '"Fred Foo 👻" <foo@example.com>', // sender address
    to: "bar@example.com, baz@example.com", // list of receivers
    subject: "Hello asdfasdf ✔", // Subject line
    text: "Hello worldasdasdasd?", // plain text body
    html: '<b>Hello worldasdasdasd?</b><br/> <img src="cid:batman/>', // html body
    attachments: [
        {
            // file on disk as an attachment
            filename: 'test.txt',
            path: __dirname+'/test.txt', // stream this file
            cid: "batman"
        },
    ]
  });

  console.log("Message sent: %s", info.messageId);
  // Message sent: <b658f8ca-6296-ccf4-8306-87d57a0b4321@example.com>

  // Preview only available when sending through an Ethereal account
  console.log("Preview URL: %s", nodemailer.getTestMessageUrl(info));
  // Preview URL: https://ethereal.email/message/WaQKMgKddxQDoou...
}
*/